[project]
name = "webmoon"
version = "0.1.0"
description = "Moontool CLI served over the web."
requires-python = ">=3.12"
dependencies = ["fastapi[standard-no-fastapi-cloud-cli]"]

[tool.uv]
package = false
